{
  "$schema": "https://railway.app/railway.schema.json",
  "$service": "rag-service",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd rag_service && uvicorn app:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}

